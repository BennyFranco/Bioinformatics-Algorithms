__author__ = 'Benny'
import math
import numpy as np


'''a = np.loadtxt('input.txt', skiprows=0, unpack=True)
sum = 0

for sn in a:
    #print (-(sn[0]*math.log2(sn[0]) + sn[1]*math.log2(sn[1]) + sn[2]*math.log2(sn[2]) + sn[3]*math.log2(sn[3])))
    for snn in sn:
        if snn !=0:
            sum += -(snn*math.log2(snn))
print('{0:.4f}'.format(sum))
    #sum=0
#print(-(0.2*math.log2(0.2) + 0.6*math.log2(0.6) + 0.2*math.log2(0.2)))'''

print(-(0.25*math.log2(0.25)))